_________________1.Dependencies______________________________________________________
install all the dependencies using the command in terminal
    npm install express mysql2 sharp mime-types express-fileupload 

    

_________________2.Creating Database______________________________________________________
create a database 'resumedb' and create a table with coloumns using the following query
    CREATE TABLE userdata(
         UserId varchar(255),
         Name varchar(255),
         Age varchar(255),
         Mobile varchar(255),
         Email varchar(255),
         City varchar(255)
     );

    

    